import React from 'react';
import GameBoard from '../containers/GameboardContainer'
import '../index.css'

const App = (props)=> {
    return (
      <div className="App">
        <GameBoard />
      </div>
    );
}

export default App;
