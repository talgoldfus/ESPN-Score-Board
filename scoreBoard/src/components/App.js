import React from 'react';
import GameBoard from '../containers/GameboardContainer'

const App = (props)=> {
    return (
      <div className="App">
        <GameBoard />
      </div>
    );
}

export default App;
